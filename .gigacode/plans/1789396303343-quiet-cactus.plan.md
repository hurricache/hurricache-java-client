# План: RawValuesClusterTest — Кластерные тесты для скаляров

## 1. Анализ требований

### Ключевые правила:
1. **KeyHintData** — каждый тест использует hint, возвращаемый методом `create`
2. **100ms на репликацию** — после `create` всегда `Thread.sleep(100)`
3. **Проверка репликации** — каждая write операция проверяет репликацию на 2-ю ноду
4. **Pattern write → check**:
   - Write на мастере → проверка на мастере (без задержки) → проверка на бэкапе (с задержкой 100ms)
   - Write на бэкапе → проверка на бэкапе (без задержки) → проверка на мастере (с задержкой 100ms)
5. **Write операции**: `setTtl`, `getAndDeleteValue`, `updateKeyValue`, `remove`
6. **Отдельные тесты** для setTtl и lock на мастере и на бэкапе
7. **TTL expiration** — проверить истечение на мастере и на бэкапе
8. **Lock expiration** — проверить истечение на мастере и на бэкапе

## 2. Структура тестов

### Секция 1: setTtl operations
| Тест | Описание |
|------|----------|
| `testSetTtlOnMaster` | create → write setTtl на master → check TTL на master → check TTL на backup |
| `testSetTtlOnBackup` | create → write setTtl на backup → check TTL на backup → check TTL на master |

### Секция 2: getAndDeleteValue operations
| Тест | Описание |
|------|----------|
| `testGetAndDeleteValueOnMaster` | create → write getAndDeleteValue на master → check result на master → check NOT_FOUND на backup |
| `testGetAndDeleteValueOnBackup` | create → write getAndDeleteValue на backup → check result на backup → check NOT_FOUND на master |

### Секция 3: updateKeyValue operations
| Тест | Описание |
|------|----------|
| `testUpdateKeyValueOnMaster` | create → write updateKeyValue на master → check value на master → check value на backup |
| `testUpdateKeyValueOnBackup` | create → write updateKeyValue на backup → check value на backup → check value на master |

### Секция 4: remove operations
| Тест | Описание |
|------|----------|
| `testRemoveOnMaster` | create → write remove на master → check removed на master → check NOT_FOUND на backup |
| `testRemoveOnBackup` | create → write remove на backup → check removed на backup → check NOT_FOUND на master |

### Секция 5: setTtl + expiration
| Тест | Описание |
|------|----------|
| `testSetTtlExpirationOnMaster` | create → setTtl(2000ms) на master → check TTL>0 на master → check TTL>0 на backup → sleep(3000) → check NOT_FOUND на master → check NOT_FOUND на backup |
| `testSetTtlExpirationOnBackup` | create → setTtl(2000ms) на backup → check TTL>0 на backup → check TTL>0 на master → sleep(3000) → check NOT_FOUND на backup → check NOT_FOUND на master |

### Секция 6: Lock operations
| Тест | Описание |
|------|----------|
| `testLockObjectOnMaster` | create → lockObject на master → check lock на master → check lock на backup → unlock |
| `testLockObjectOnBackup` | create → lockObject на backup → check lock на backup → check lock на master → unlock |

### Секция 7: Lock expiration
| Тест | Описание |
|------|----------|
| `testLockExpirationOnMaster` | create → lockObject(2000ms) на master → check lock на master → check lock на backup → sleep(3000) → check lock expired на master → check lock expired на backup |
| `testLockExpirationOnBackup` | create → lockObject(2000ms) на backup → check lock на backup → check lock на master → sleep(3000) → check lock expired на backup → check lock expired на master |

## 3. Константы
```java
private static final int OWNER_CLIENT_ID = 100;
private static final int INTRUDER_CLIENT_ID = 200;
private static final long REPLICATION_DELAY_MS = 100;
```

## 4. Файл для модификации
| Файл | Действие |
|------|----------|
| `src/test/java/.../smart/RawValuesClusterTest.java` | Добавить новые тесты |

## 5. Верификация
1. **Компиляция:** `mvn test-compile -q` — без ошибок ✅
2. **Запуск тестов:** `mvn test -Dtest=RawValuesClusterTest -q` — все тесты прошли ✅

## 6. Готово

### Добавленные тесты:
- Секция 1: `testSetTtlOnMaster`, `testSetTtlOnBackup` ✅
- Секция 2: `testGetAndDeleteValueOnMaster`, `testGetAndDeleteValueOnBackup` ✅
- Секция 3: `testUpdateKeyValueOnMaster`, `testUpdateKeyValueOnBackup` ✅
- Секция 4: `testRemoveOnMaster`, `testRemoveOnBackup` ✅
- Секция 5: `testSetTtlExpirationOnMaster`, `testSetTtlExpirationOnBackup` ✅
- Секция 6: `testLockObjectOnMaster`, `testLockObjectOnBackup` ✅
- Секция 7: `testLockExpirationOnMaster`, `testLockExpirationOnBackup` ✅

### Исправленные старые тесты:
- `testCreateScalarOnMaster/Backup` — KeyHintData, 100ms ✅
- `testUpdateScalarOnMaster/Backup` — KeyHintData, 100ms ✅
- `testExistKeyOnMaster` — удалён дубликат, добавлен новый с KeyHintData ✅
- `testSetTtlAndGetTtlOnMaster` — KeyHintData, 100ms ✅
- `testTtlExpirationOnMaster/Backup` — KeyHintData, 100ms ✅
- `testLockObjectReadLock/WriteLock/GlobalLock` — KeyHintData, 100ms ✅
- `testUnlockObjectOwnerOnly` — KeyHintData, 100ms ✅
- `testRemoveOnMaster/Backup` — исправлена проверка повторного удаления (NOT_FOUND) ✅

### Общие правила:
- `createKeyValue` без `setMode`
- После create — 100ms на репликацию
- Write на ноде → проверка на ней → проверка на соседней (с 100ms)
- `bytes(key)` вместо `.getBytes(StandardCharsets.UTF_8)`
- `assertDenied()` вместо try-catch для PERMISSION_DENIED