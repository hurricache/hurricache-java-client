# План: Анализ покрытия и создание HashMapOperationsTest

## 1. Анализ текущего покрытия HashedMap (MAP container)

### Текущие тесты, использующие MAP контейнер:
| Файл | Что тестирует |
|------|---------------|
| `ContainerMapOperationsTest.java` | createMap, streamMap, getContainerValue, updateContainerValue, containsContainerKey, getAndRemoveContainerValue, removeFromContainer(key), addElementHashMap |
| `ContainerMapEdgeCasesTest.java` | Edge cases: несуществующие ключи, NOT_FOUND ошибки |
| `MapAndSetCollectionsTest.java` | createMap, streamMap, getSize (базовые) |

### НЕ ПОКРЫТЫЕ случаи (gap analysis):

| № | Метод | Статус | Примечание |
|---|-------|--------|------------|
| 1 | `createMap` пустой | ❌ | Нет теста создания пустого HashedMap |
| 2 | `createMap` с большими данными (чанкинг) | ❌ | Нет теста chunking для MAP |
| 3 | `streamMap` пустой | ❌ | Нет теста streamMap пустого контейнера |
| 4 | `streamMap` с данными | ⚠️ | Базовое покрытие есть, но нет с clientId |
| 5 | `addElementHashMap` дубликаты | ❌ | MAP **не допускает** дубликаты — повторный ключ не добавится, вернёт 0 |
| 6 | `removeFromContainer(key)` пустой map | ❌ | NOT_FOUND для пустого контейнера |
| 7 | `removeFromContainer(ContainerType, keys, values)` для MAP | ❌ | Второй overload метода |
| 8 | `removeElementAtPosition` на MAP | ❌ | Должен вернуть ошибку (не поддерживается) |
| 9 | `remove` контейнера | ❌ | Удаление всего контейнера |
| 10 | `remove` несуществующего контейнера | ❌ | Должен вернуть NOT_FOUND |
| 11 | `getSize` для MAP | ❌ | Нет тестов getSize для MAP |
| 12 | `setTtl` для MAP | ❌ | Установка TTL на контейнер |
| 13 | `getTtl` для MAP | ❌ | Получение TTL контейнера |
| 14 | TTL expiration для MAP | ❌ | После истечения TTL get вернет NOT_FOUND |
| 15 | `lockObject` для MAP | ❌ | Блокировки с clientId |
| 16 | `unlockObject` для MAP | ❌ | Разблокировка владельцем/не владельцем |
| 17 | Методы не применимые к MAP | ❌ | getElementAtPosition, getHead, getTail и т.д. |

---

## 2. Создание HashMapOperationsTest

### Расположение:
`/ext/hurricache-java-client/src/test/java/com/hurricache/client/standalone/simple/HashMapOperationsTest.java`

### Наследование:
```java
public class HashMapOperationsTest extends TestBase
```

### Структура тестов (12 секций):

#### Секция 1: Создание контейнера (createMap)
| Тест | Описание |
|------|----------|
| `testCreateEmptyMap` | Создание пустого HashedMap через `createMap(key, Map.of())` |
| `testCreateMapWithInitialData` | Создание с начальными данными (2-3 элемента) |
| `testCreateMapWithLargeDataChunking` | Создание с 1500+ элементами для проверки чанкинга |

#### Секция 2: streamMap
| Тест | Описание |
|------|----------|
| `testStreamMapEmpty` | Пустой HashedMap → пустой Map |
| `testStreamMapWithData` | HashedMap с данными → возвращает все содержимое |
| `testStreamMapWithClientId` | streamMap с явным clientId |

#### Секция 3: addElementHashMap
| Тест | Описание |
|------|----------|
| `testAddElementHashMap` | Добавление элементов, возврат количества |
| `testAddElementHashMapNoDuplicates` | MAP **не допускает** дубликаты — повторный ключ → 0 добавлено |
| `testAddElementHashMapEmptyList` | Пустой список → 0 добавлено |

#### Секция 4: removeFromContainer (по ключу)
| Тест | Описание |
|------|----------|
| `testRemoveFromContainerByKey` | Удаление существующего элемента → 1 |
| `testRemoveFromContainerByKeyNotFound` | Удаление несуществующего → NOT_FOUND |
| `testRemoveFromContainerFromEmptyMap` | Удаление из пустого контейнера → NOT_FOUND |

#### Секция 5: removeFromContainer (по ключу и значению, ContainerType)
| Тест | Описание |
|------|----------|
| `testRemoveFromContainerWithType` | Удаление через `removeFromContainer(key, hint, ContainerType.MAP, keys, values)` → 1 |
| `testRemoveFromContainerWithTypeNotFound` | Удаление по неверному ключу/значению → 0 |

#### Секция 6: containsContainerKey
| Тест | Описание |
|------|----------|
| `testContainsContainerKeyExists` | Существующий ключ → true |
| `testContainsContainerKeyNotExists` | Несуществующий ключ → false |

#### Секция 7: removeElementAtPosition (ошибка)
| Тест | Описание |
|------|----------|
| `testRemoveElementAtPositionNotSupported` | Вызов `removeElementAtPosition` на MAP → ошибка |

#### Секция 8: remove контейнера
| Тест | Описание |
|------|----------|
| `testRemoveExistingContainer` | Удаление существующего контейнера → true |
| `testRemoveNonExistentContainer` | Удаление несуществующего → NOT_FOUND |

#### Секция 9: getSize
| Тест | Описание |
|------|----------|
| `testGetSizeEmptyMap` | Пустой → 0 |
| `testGetSizeWithElements` | С элементами → корректное количество |

#### Секция 10: getContainerValue и updateContainerValue
| Тест | Описание |
|------|----------|
| `testGetContainerValue` | Получение значения по ключу |
| `testGetContainerValueNotFound` | Несуществующий ключ → NOT_FOUND |
| `testUpdateContainerValue` | Обновление → возврат старого значения |
| `testUpdateContainerValueNotFound` | Обновление несуществующего → NOT_FOUND |

#### Секция 11: getAndRemoveContainerValue
| Тест | Описание |
|------|----------|
| `testGetAndRemoveContainerValue` | Извлечение + удаление → старое значение |
| `testGetAndRemoveContainerValueNotFound` | Несуществующий → NOT_FOUND |

#### Секция 12: TTL операции
| Тест | Описание |
|------|----------|
| `testSetTtl` | Установка TTL на контейнер |
| `testGetTtl` | Получение TTL после установки |
| `testTtlExpiration` | После истечения TTL → get вернет NOT_FOUND (sleep 1.5с при TTL=1) |

#### Секция 13: Блокировки (lock/unlock)
| Тест | Описание |
|------|----------|
| `testReadLockParallelReads` | READ_LOCK: несколько клиентов читают параллельно |
| `testWriteLockExclusiveAccess` | WRITE_LOCK: только владелец читает/пишет |
| `testGlobalLockExclusiveAccess` | GLOBAL: только владелец, все остальные blocked |
| `testUnlockByOwnerOnly` | unlockObject: только владелец может разблокировать |
| `testUnlockByWrongClient` | unlockObject не владельцем → CANT_LOCK / assertDenied |

#### Секция 13.1: Истечение блокировки (lock expiration)
| Тест | Описание |
|------|----------|
| `testReadLockExpiration` | READ_LOCK на 2 сек → Thread.sleep(3000) → лок снят, другой клиент может читать |
| `testWriteLockExpiration` | WRITE_LOCK на 2 сек → Thread.sleep(3000) → лок снят, intruder получает WRITE_LOCK |
| `testGlobalLockExpiration` | GLOBAL на 2 сек → Thread.sleep(3000) → лок снят, intruder получает WRITE_LOCK |
| `testReadLockBlocksWrites` | READ_LOCK: владелец читает OK, но пишет → assertDenied (PERMISSION_DENIED) |
| `testLockExpirationAllowsNewLock` | Любой lock на 2 сек → sleep 3с → новый lock OK для другого клиента |

#### Секция 14: Методы не применимые к MAP
| Тест | Описание |
|------|----------|
| `testUnsupportedMethodsForMap` | getElementAtPosition, getHead, getTail, removeElementAtPosition → ошибка |

---

## 3. Ключевые конвенции

### Константы:
```java
private static final Duration TEST_TIMEOUT = Duration.ofSeconds(2);
private static final int DEFAULT_CLIENT_ID = 101;
private static final int OWNER_CLIENT_ID = 100;
private static final int INTRUDER_CLIENT_ID = 200;
```

### Утилиты:
```java
private byte[] bytes(String val) { return val.getBytes(StandardCharsets.UTF_8); }
private Payload p(String val) { return Payload.of(val.getBytes(StandardCharsets.UTF_8)); }
private String str(Payload payload) { return new String(payload.getValue(), StandardCharsets.UTF_8); }
```

### Паттерн ожидания NOT_FOUND:
```java
ExecutionException ex = Assertions.assertThrows(
    ExecutionException.class,
    () -> client.someMethod(...).get()
);
Assertions.assertEquals(Status.Code.NOT_FOUND, ((StatusRuntimeException) ex.getCause()).getStatus().getCode());
```

### Паттерн assertDenied (перехват PERMISSION_DENIED / Access Denied by Lock):
```java
// Для операций чтения/записи под блокировкой — intruder получает PERMISSION_DENIED
assertDenied(client.someMethod(..., INTRUDER_CLIENT_ID, ...));

// Для unlockObject не владельцем — получает CANT_UNLOCK (LockStatus)
LockStatus status = client.unlockObject(key, INTRUDER_CLIENT_ID).get();
assertEquals(LockStatus.CANT_UNLOCK, status);
```

**assertDenied** проверяет:
- `ExecutionException` → `Status.Code.PERMISSION_DENIED` + "Access Denied by Lock" в описании
- `LockStatus` результат → не `OK`

### Паттерн ожидания CANT_UNLOCK:
```java
LockStatus status = client.unlockObject(key, wrongClientId).get();
assertEquals(LockStatus.CANT_UNLOCK, status);
```

### Паттерн истечения блокировки (lock expiration):
```java
// Берём lock с TTL = 2 секунды
LockStatus lock = client.lockObject(key, LockType.READ_LOCK, OWNER_CLIENT_ID, Duration.ofSeconds(2)).get();
assertEquals(LockStatus.OK, lock);

// Ждём истечения
Thread.sleep(3000);

// Теперь другой клиент может получить lock
LockStatus newLock = client.lockObject(key, LockType.WRITE_LOCK, INTRUDER_CLIENT_ID, Duration.ofSeconds(30)).get();
assertEquals(LockStatus.OK, newLock);

client.unlockObject(key, INTRUDER_CLIENT_ID).get();
```

---

## 4. Файлы для модификации

| Файл | Действие |
|------|----------|
| `src/test/java/.../HashMapOperationsTest.java` | **Создать** — новый файл со всеми тестами |

---

## 5. Верификация

1. **Компиляция:** `mvn compile test-compile -q` — без ошибок
2. **Запуск тестов:** `mvn test -Dtest=HashMapOperationsTest -q` — все тесты проходят
3. **Проверка покрытия:** Убедиться что все 14 секций покрыты тестами

---

## 6. Примечания по реализации

- Все тесты с блокировками вызывают методы с `clientId` параметром
- TTL expiration тесты используют `Thread.sleep(1500)` при TTL=1
- Для `removeFromContainer` с `ContainerType.MAP`:
  ```java
  List<Payload> keys = List.of(Payload.of(elementKey));
  List<Payload> values = List.of(Payload.of(elementValue));
  client.removeFromContainer(keyBytes, hint, ContainerType.MAP, keys, values).get();
  ```
- `addElementHashMap` для MAP **не допускает дубликаты** — повторный ключ возвращает 0 добавленных
- `removeFromContainer(key)` для MAP **не может вернуть > 1** (нет дубликатов по ключу в элементе)