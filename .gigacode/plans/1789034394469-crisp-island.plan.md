# Plan: QueueOperationsTest — Comprehensive Rewrite

## Objective
Rewrite `QueueOperationsTest.java` to cover all queue operations comprehensively, following the pattern of `ListOperationsTest.java` and `VectorOperationsTest.java`.

**Current state:** 3 tests, minimal coverage
**Target state:** ~50 tests covering all queue operations, edge cases, locks, TTL, and unsupported methods

## Queue API Constraints (UPDATED)

| Method | Supported? | Notes |
|--------|-----------|-------|
| `createQueue` | ✅ | Empty or with initial data |
| `streamQueue` | ❌ | Not supported |
| `getAndRemoveFront` | ✅ | Pop front |
| `getFront` / `getHead` | ✅ | Peek front/head |
| `getTail` | ✅ | Peek tail |
| `getAndRemoveTail` | ✅ | Pop back |
| `addElementToTail` | ✅ | Push back |
| `addElementToHead` | ✅ | **Push front (DEPRECATED but supported)** |
| `removeHead` | ✅ | Pop front without return |
| `removeTail` | ✅ | **Pop back without return (supported)** |
| `getSize` | ❌ | **Returns 0 (not supported)** |
| `getElementAtPosition` | ❌ | Returns INTERNAL "Key not found or type is not correct" |
| `getAndRemoveElementAtPosition` | ❌ | Returns INTERNAL |
| `addElementToPosition` | ❌ | Returns INTERNAL |
| `removeElementAtPosition` | ❌ | Returns INTERNAL |
| `addElementToPositionBefore` | ❌ | Returns INTERNAL |
| `addElementToPositionAfter` | ❌ | Returns INTERNAL |
| `remove` | ✅ | Delete queue |
| `lockObject` / `unlockObject` | ✅ | Lock types supported |
| `setTtl` / `getTtl` | ✅ | TTL supported |

**Unsupported methods error:** `Status.Code.INTERNAL` with message "Key not found or type is not correct"

**Empty queue behavior:**
- `getHead` returns empty Payload (empty byte array)
- `removeHead` does NOT throw, returns normally

**getSize behavior:** Returns 0 for queues (not supported, but doesn't throw).

## Test Class Structure

**File:** `/ext/hurricache-java-client/src/test/java/com/hurricache/client/standalone/simple/QueueOperationsTest.java`

**Helper method:**
```java
private void assertLockDenied(CompletableFuture<?> future) {
    // Try future.get(), catch ExecutionException, verify Status.Code.PERMISSION_DENIED
    // and "Access Denied by Lock" in description
}

private void assertUnsupportedMethod(CompletableFuture<?> future) {
    // Try future.get(), catch ExecutionException, verify Status.Code.INTERNAL
    // and "Key not found or type is not correct" in description
}
```

## Test Sections

### 1. CREATE QUEUE (3 tests)
| # | Test | Description |
|---|------|-------------|
| 1.1 | `testCreateEmptyQueue` | Create empty queue, verify getHead returns empty |
| 1.2 | `testCreateQueueWithInitialData` | Create queue with 3 elements, verify order via getAndRemoveFront |
| 1.3 | `testCreateLargeQueueWithChunking` | Create queue with 1500 large elements (8KB each), verify count |

### 2. EMPTY QUEUE EDGE CASES (2 tests)
| # | Test | Description |
|---|------|-------------|
| 2.1 | `testGetHeadOnEmptyQueue` | Create empty queue, getHead returns empty Payload |
| 2.2 | `testGetAndRemoveFrontOnEmptyQueue` | Create empty queue, getAndRemoveFront returns empty Payload |

### 3. GET AND REMOVE FRONT (2 tests)
| # | Test | Description |
|---|------|-------------|
| 3.1 | `testGetAndRemoveFront` | Dequeue front from 3-element queue, verify remaining |
| 3.2 | `testGetAndRemoveFrontOnSingleElement` | Dequeue from single-element queue (edge: empty after) |

### 4. GET FRONT / GET HEAD (2 tests)
| # | Test | Description |
|---|------|-------------|
| 4.1 | `testGetFrontAndHead` | Both return same front element, queue unchanged |
| 4.2 | `testGetFrontOnSingleElementQueue` | Edge: single element |

### 5. GET TAIL (2 tests)
| # | Test | Description |
|---|------|-------------|
| 5.1 | `testGetTail` | Get tail from 3-element queue |
| 5.2 | `testGetTailOnSingleElementQueue` | Edge: single element = head == tail |

### 6. GET AND REMOVE TAIL (2 tests)
| # | Test | Description |
|---|------|-------------|
| 6.1 | `testGetAndRemoveTail` | Pop back from 3-element queue, verify remaining |
| 6.2 | `testGetAndRemoveTailOnSingleElementQueue` | Edge: single element, queue becomes empty |

### 7. ADD ELEMENT TO TAIL (1 test)
| # | Test | Description |
|---|------|-------------|
| 7.1 | `testAddElementToTail` | Add element to existing queue, verify order |

### 8. ADD ELEMENT TO HEAD (2 tests) — NEW
| # | Test | Description |
|---|------|-------------|
| 8.1 | `testAddElementToHead` | Prepend element to queue, verify it becomes new head |
| 8.2 | `testAddElementToHeadMultiple` | Prepend multiple elements, verify order (last prepend = head) |

### 9. REMOVE HEAD (1 test)
| # | Test | Description |
|---|------|-------------|
| 9.1 | `testRemoveHead` | removeHead without return, verify next element |

### 10. REMOVE TAIL (2 tests) — NEW
| # | Test | Description |
|---|------|-------------|
| 10.1 | `testRemoveTail` | removeTail without return, verify new tail |
| 10.2 | `testRemoveTailOnSingleElement` | Edge: single element, queue becomes empty |

### 11. GET SIZE (1 test) — NEW
| # | Test | Description |
|---|------|-------------|
| 11.1 | `testGetSizeReturnsZero` | getSize on queue returns 0 (not supported) |

### 12. REMOVE QUEUE (2 tests)
| # | Test | Description |
|---|------|-------------|
| 12.1 | `testRemoveQueue` | Remove queue, verify NOT_FOUND on next operation |
| 12.2 | `testRemoveNonExistentQueue` | Remove non-existent queue, verify NOT_FOUND |

### 13. SET TTL / GET TTL (1 test)
| # | Test | Description |
|---|------|-------------|
| 13.1 | `testSetTtlAndGetTtlOnQueue` | Set TTL to 5000ms, verify getTtl > 0 |

### 14. TTL EXPIRY (1 test)
| # | Test | Description |
|---|------|-------------|
| 14.1 | `testQueueTtlExpiry` | Set TTL to 2000ms, sleep 2500ms, verify NOT_FOUND |

### 15. LOCK OPERATIONS (12+ tests)

**15a. Basic lock types (4 tests):**
| # | Test | Description |
|---|------|-------------|
| 15.1 | `testWriteLockOnQueue` | WRITE_LOCK acquire + unlock |
| 15.2 | `testReadLockOnQueue` | READ_LOCK acquire + unlock |
| 15.3 | `testGlobalLockOnQueue` | GLOBAL acquire + unlock |
| 15.4 | `testNoLockOnQueue` | NO_LOCK acquire + unlock |

**15b. READ_LOCK behavior (2 tests):**
| # | Test | Description |
|---|------|-------------|
| 15.5 | `testReadLockAllowsReadBlocksWrite` | Owner reads OK, others read OK, nobody writes (addElementToTail, addElementToHead, getAndRemoveFront, getAndRemoveTail blocked) |
| 15.6 | `testReadLockBlocksAllWriteOperations` | Test write ops blocked: addElementToTail, addElementToHead, getAndRemoveFront, getAndRemoveTail, removeHead, removeTail |

**15c. WRITE_LOCK behavior (3 tests):**
| # | Test | Description |
|---|------|-------------|
| 15.7 | `testWriteLockOwnerReadsWritesOthersBlocked` | Owner reads/writes OK, others blocked from read AND write |
| 15.8 | `testWriteLockAllReadOpsBlockedForOthers` | Test all read ops blocked for others: getFront, getHead, getTail, getAndRemoveFront, getAndRemoveTail |
| 15.9 | `testWriteLockAllWriteOpsBlockedForOthers` | Test all write ops blocked for others: addElementToTail, addElementToHead, getAndRemoveFront, getAndRemoveTail, removeHead, removeTail |

**15d. GLOBAL lock behavior (3 tests):**
| # | Test | Description |
|---|------|-------------|
| 15.10 | `testGlobalLockOwnerWorksOthersBlocked` | Owner all ops OK, others blocked from read AND write |
| 15.11 | `testGlobalLockOwnerCanRemove` | Owner removes queue, unlock returns NOT_FOUND |
| 15.12 | `testCantLockByOtherClient` | WRITE_LOCK by owner, intruder gets PERMISSION_DENIED |

### 16. UNLOCK OPERATIONS (3 tests)
| # | Test | Description |
|---|------|-------------|
| 16.1 | `testUnlockByOwner` | Owner unlocks WRITE_LOCK |
| 16.2 | `testCantUnlockByNonOwner` | Non-owner tries to unlock, PERMISSION_DENIED |
| 16.3 | `testUnlockAfterQueueDeleted` | Owner deletes queue while locked, unlock returns NOT_FOUND |

### 17. UNSUPPORTED METHODS ERROR CASES (6 tests)
| # | Test | Description |
|---|------|-------------|
| 17.1 | `testGetElementAtPositionOnQueue` | Returns Status.Code.INTERNAL "Key not found or type is not correct" |
| 17.2 | `testGetAndRemoveElementAtPositionOnQueue` | Returns INTERNAL |
| 17.3 | `testAddElementToPositionOnQueue` | Returns INTERNAL |
| 17.4 | `testRemoveElementAtPositionOnQueue` | Returns INTERNAL |
| 17.5 | `testAddElementToPositionBeforeOnQueue` | Returns INTERNAL |
| 17.6 | `testAddElementToPositionAfterOnQueue` | Returns INTERNAL |

### 18. MISSING QUEUE ERROR CASES (2 tests)
| # | Test | Description |
|---|------|-------------|
| 18.1 | `testGetAndRemoveFrontOnMissingQueue` | NOT_FOUND |
| 18.2 | `testGetTailOnMissingQueue` | NOT_FOUND |

## Total: ~50 test methods

## Key Differences from List/Vector Tests

1. **No streamQueue** — queue doesn't support streaming
2. **No positional operations** — getElementAtPosition, addElementToPosition, etc. are not supported
3. **addElementToHead IS supported** — prepend to head works
4. **removeTail IS supported** — remove from tail without return works
5. **getSize returns 0** — not supported, returns 0 silently (doesn't throw)
6. **Empty queue returns empty Payload** — not an error for getHead/getAndRemoveFront
7. **Unsupported methods return Status.Code.INTERNAL** — not NOT_FOUND
8. **removeHead doesn't throw on empty queue** — returns normally

## Files to Create/Modify

| File | Action |
|------|--------|
| `/ext/hurricache-java-client/src/test/java/com/hurricache/client/standalone/simple/QueueOperationsTest.java` | **REWRITE** — comprehensive test class |

## Verification

1. Run the test class: `mvn test -Dtest=QueueOperationsTest`
2. Verify all ~50 tests pass
3. Ensure no interference with existing QueueOperationsReplicationTest.java
